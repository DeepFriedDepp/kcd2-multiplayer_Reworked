# WO-102.5 — progress

Session 2026-09-18. Findings: `docs/WO-102.5-findings.md`. Builds on WO-102
(0.24.0, host authority shipped on).

## Phases

| phase | state | evidence |
|---|---|---|
| 0 — what WO-102 landed | **done, verification only** — non-owner already stops emitting at the source (no fix needed); scan/cap/radius confirmed against the current `kdcmp.lua`, not just the doc | (code-verified) against d333654 |
| 1 — pause on by default + resume guarantees | **shipped `mp_authority_pause_on`**; resume added on `mp_stop`, agent disconnect, and a 5s periodic reconciliation sweep (peer disconnect's own ~3s silence-release path already covered it); save-persistence question narrowed (`C_IntelligentObject::Save` exists) but still (inconclusive) — mitigated by the sweep either way | (observed, WO-102's own 8/8 solo probe, unchanged); (synthetic) Lua 135/135 incl. 10 new |
| 2 — native NPC scan | **built behind `mp_npc_scan_native_on` (off)**: `native/KCDMP/npc_scan.{h,cpp}`, pipe `0x0B→0x87`, agent orchestration + name-only push, Lua consumption in `mp_npc_rescan` (one substitution point, policy untouched), `mp_npc_scan_compare` known-answer check | (code-verified) Ghidra this session, findings §2.1; (synthetic) Lua 125/125 incl. 16 new, agent 156/156 incl. 6 new; NOT live-verified |

(Phases 3, 4, 5 and the end gate: not yet reached this session — updated
here as they land.)

## Commits (`WO-102.5:` on `origin main`)

1. Phase 0 — assessment only, no code (findings §0).
2. Phase 2 — native scan (native + pipe + agent + Lua + tests), findings §2.
3. Phase 1 — pause default + resume guarantees (Lua + one GameBridge.cs call
   site + tests), findings §1.

## Baseline before any change (observed this session)

* `cmake --build native/build --target KCDMP`: green, 383,488 bytes before
  Phase 2 (unchanged from WO-102's own build).
* `dotnet test KcdMp.Client.Tests`: 150/150. `KcdMp.Relay.Tests`: 12/12 (one
  flaky failure on a cold first run, 12/12 on immediate retry — a real-TCP
  test timing artefact, not a regression; noted, not chased further).
* `tools/Test-WO102Synthetic.ps1`: 109/109. Every other Lua synthetic suite
  green (35, 48, 33, 72, 47, 70, 101, 32, 160, 50) except
  `Test-WO99Synthetic.ps1`, which is the pre-existing parked "always exits 2"
  issue (durable context), not touched this session.

## Not done, and why

* No live game ran. Everything native is gated and defaults off, same risk
  posture as WO-102's own `local_state.cpp` on its first session.
* No per-path cadence comparison for the scan (findings §2.3 item 5) — a real
  gap against the WO's own "measure both paths" ask, named rather than
  papered over.
