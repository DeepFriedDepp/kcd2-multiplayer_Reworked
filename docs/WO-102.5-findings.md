# WO-102.5 — native NPC scan, uncapped co-located ownership

Session 2026-09-18. Builds on WO-102 (host authority shipped ON in 0.24.0).
Evidence marks, strict: **(observed)** = a log or a screen; **(code-verified)**
= read out of source, a binary or a shipped data file; **(synthetic)** = a
test harness, no game; **(inconclusive)** = exactly that. Nothing rounded up.

No game ran this session (same constraint as WO-102's own session: the
environment this runs in cannot launch/inject into the game). Everything
below is (code-verified) or (synthetic) unless marked otherwise; every native
read is gated and defaults to the Lua fallback rather than trusted blind.

---

## 0. Phase 0 — what WO-102 actually landed (verification only, no code)

Read `docs/WO-102-findings.md` and the current `kdcmp.lua` directly rather
than trusting the doc's prose alone (code-verified against d333654):

1. **Host authority and the pause lever, and which toggles.** Confirmed:
   `mp_authority_host_on|off` ships **on** (`KCD2MP.wo102.authorityHost =
   true`); `mp_authority_pause_on|off` ships **off**
   (`KCD2MP.wo102.authorityPause = false`), gated separately.
2. **Is the stream genuinely one-directional?** Yes, at the source, not just
   muted at the relay. `KCD2MP_NpcSyncTick` (kdcmp.lua:3457-3461, `if
   KCD2MP.wo102.authorityHost then return end`): under host authority a
   non-authority returns before the drag sensor AND before the proximity
   claim, every tick. It never calls `KCD2MP_EmitEvent("npc_claim", ...)` or
   `"npc_drag"`. **No fix needed** — WO-102 already did this correctly; the
   relay-side muting (`RouteNpcState`) is defence in depth for a mixed
   claim/host pair (WO-102 S4.5), not the only thing stopping a non-authority
   under host authority.
3. **The current scan.** `mp_npc_rescan` (kdcmp.lua): sphere around the local
   player and every peer ghost (`anchors`), cap `KCD2MP.npcSync.maxTracked *
   #anchors` (5 per anchor), `enterRadius = KCD2MP.npcSync.radius` (30 m),
   `exitRadius = enterRadius * NPC_TRACK_EXIT_FACTOR` (1.5, so 45 m).
   Confirmed exactly as WO-102 §4.2 describes.
4. **The request channel.** Unchanged this session; not touched by this WO.
   `CombatModule 0x76500` remains a STOP on an NPC (WO-100.5); resolution
   still goes through the existing damage path (WO-102 §5.3).
5. Host authority landed. Proceeding to Phase 2.
